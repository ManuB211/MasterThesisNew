(ns c3pro-editor.gui.menu
  (:require [cbsbot.core :as object]
            [cbsbot.macros :refer [defgui]]))

(defgui menu-gui
  :init (fn []
          ; TODO: this cannot be defined at this point. only during
          ; menu/object construction.
          nil))

